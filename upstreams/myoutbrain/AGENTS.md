## Agent skills

### Issue tracker

本仓库使用 `.scratch/` 下的本地 Markdown 文件跟踪事项。详见 `docs/agents/issue-tracker.md`。

### Triage labels

事项分流使用五个默认的规范状态字符串。详见 `docs/agents/triage-labels.md`。

### Domain docs

领域文档采用单上下文（single-context）布局。详见 `docs/agents/domain.md`。
