.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
*.egg-info/

# A source checkout is not a private MyOutBrain instance. Keep all canonical
# memory, evidence, projections, Obsidian views, and instance configuration in
# a separate local instance root created with `myoutbrain init`.
/myoutbrain.toml
/.myoutbrain.lock
/store/
/runtime/
/vault/
