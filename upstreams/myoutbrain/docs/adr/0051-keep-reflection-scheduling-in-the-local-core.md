# 由本地核心调度反思运行

V2 允许显式请求、实质任务边界和已配置计划触发反思，但由本地核心拥有触发器、计划、授权、持久运行状态、重试和通知。Skill 只编排一次有界反思，智能体入口只报告任务边界；Skill 不被视为能自行常驻或可靠后台醒来的服务。所有触发方式最多生成审阅提案，普通闲聊和简单操作默认跳过，语义批准仍由创作者完成。

## Consequences

不同智能体入口可以复用同一反思流程而不各自维护定时器或积压队列。计划运行可以延续第一阶段已有的本地恢复、云端授权和通知边界，同时更换 Skill 或能力引擎不会丢失运行状态。
