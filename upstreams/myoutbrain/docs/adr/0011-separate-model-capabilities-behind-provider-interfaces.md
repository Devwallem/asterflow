# 按模型能力分离提供方接口

MyOutBrain 将文本生成、嵌入及未来的重排、转录等能力置于彼此独立的提供方接口之后，OpenAI 是第一组适配器而不是领域逻辑依赖；文本生成使用 Responses API，嵌入使用独立的 Embeddings API。每份可重建索引记录提供方、模型、维度和切片版本，模型变化时重建并原子切换索引，禁止在同一索引中混用不同向量空间，从而支持以后替换模型或接入其他 API。
