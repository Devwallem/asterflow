# 按依赖组执行审阅批次

V2 先把一次自然语言批审解析为不可变且可重放的审阅批次，并统一校验提案编号、权限、目标版本与依赖。无依赖提案分别以幂等事务执行，允许无关项部分成功；存在先后依赖的提案组成原子组。失败项保持待审并携带原因，重复提交同一批次不会重复写入，每项明确记录接受、编辑、拒绝、暂缓或冲突结果。

## Consequences

批量审阅不会因为一个无关冲突全部回滚，也不会留下无法解释的随机半完成状态。执行器需要持久化批次身份、提案预期版本、依赖关系和逐项结果，以支持安全恢复和重试。
