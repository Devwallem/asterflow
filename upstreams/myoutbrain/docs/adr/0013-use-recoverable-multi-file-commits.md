---
status: superseded by ADR-0080
---

# 对多文件知识晋升使用可恢复事务

MyOutBrain 由 Python 后台在项目锁内写入 Vault 笔记和演变事件：每个目标采用原子替换，多目标由持久事务清单协调，任何后续 MyOutBrain 操作都先持锁恢复未完成事务，因而只观察到完整的提交前或提交后状态。Obsidian CLI 不参与持久化提交，只在成功提交后打开笔记；这是为了同时保留可恢复的一致性与普通 Markdown 的可移植性，而不把数据完整性委托给界面适配器。
