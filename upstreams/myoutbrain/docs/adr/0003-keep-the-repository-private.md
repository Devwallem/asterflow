# 永久保持仓库私有

MyOutBrain 及其 Git 历史永久保持私有；Git 保存代码、配置、领域文档、Vault Markdown 和轻量永久记录，但不保存原始对象或 `runtime/` 下的可重建数据。若未来需要开源程序代码，将创建不含私人历史的新仓库，而不是公开当前仓库。原始对象和运行数据的备份方式后来由 ADR-0098 改为主私人实例整目录冷 ZIP 快照，不再要求独立加密备份。
