# 阻止依赖不完整或受限的知识迁移

V2 以待迁移知识及其证据和知识依赖闭包作为迁移审计单位，并采用 fail-closed 策略。只要闭包中存在不可导出、无法审计、缺失或许可不明的来源，或者知识正文可能泄露受限依赖，整个迁移单元就被阻止；初版不允许通过删除来源、标记 provenance gap 或自动脱敏继续携带结论。审计只输出阻止原因、受影响关系和可行修复方向。

## Consequences

需要跨环境使用的内容必须重新形成只依赖允许来源的干净知识并经过审阅，不能用用户覆盖警告绕过闭包检查。该保守边界可以在未来依据真实迁移案例重新设计，但 V2 不实现部分证据迁移。
