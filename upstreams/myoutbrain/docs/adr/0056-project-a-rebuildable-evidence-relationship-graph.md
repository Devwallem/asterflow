# 投影可重建的证据关系图

V2 持久保存知识版本与来源之间的规范关系，并从 `supports`、`contradicts`、`derived-from`、`validated-by` 和 `supersedes` 等关系生成可重建的双向证据关系图。关系图索引不是事实来源；删除后可以从规范关系重建。来源内容、可用性或时效发生变化时，系统通过关系图定位受影响知识并生成复核提案，不自动改写规范记忆。

## Consequences

知识更新和证据验证可以从任一方向追踪影响范围，旧版本仍保留其历史适用期。实现不得把所有语义相似候选永久连接成知识图，也不得让索引丢失导致来源关系消失。
