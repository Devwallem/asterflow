---
status: superseded by ADR-0093
---

# 允许计划任务调用云端分析但不提交语义合并

MyOutBrain 的手动或定时计划整合可以在创作者预先配置提供方、模型、允许的数据敏感度、批次、Token 与费用上限后调用云端能力，授权可撤销且 `local_only` 内容始终排除；每次外发记录证据 ID、用途、成本和结果。无人值守任务可以完成候选召回、语义比较、影响分析和整合提案，哈希去重、可重建索引与其他不改变语义的机器维护可以自动提交，但修改规范记忆正文、取代旧理解、解决冲突、晋升个人认知、停用或删除长期内容都必须等待创作者批准。
