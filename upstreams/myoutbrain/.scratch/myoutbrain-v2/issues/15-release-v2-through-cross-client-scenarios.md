# 15 — 通过跨客户端黑盒场景发布 V2

**What to build:** 创作者可以通过三种智能体入口完成同一个捕获、审阅、召回、反证、迁移和恢复闭环，旧事实来源旁路被移除，V2 达到可发布状态。

**Blocked by:** 09 — 发现反证并在审阅前保持知识不变; 10 — 无损裂分与合并知识胶囊; 11 — 安装三种入口并协商协议能力; 12 — 跨入口领取计划反思; 13 — 迁移经审计的知识闭包; 14 — 整库备份、诊断与垃圾回收.

**Status:** resolved

- [x] 规格中的九项发布阻塞黑盒场景全部自动通过。
- [x] 三入口召回同一知识版本、写入同构召回日志并共享审阅状态。
- [x] 无 Embedding、无网络、无常驻进程时基础闭环通过。
- [x] V1 内部布局测试被行为等价的 V2 高层测试替代，不保留第二规范事实来源。
- [x] 所有取代 ADR、领域词汇、协议 Schema 和兼容矩阵与实现一致。
- [x] 发布说明明确 V2 非目标和后续可选能力。

## Answer

- 新增九项 V2 发布阻塞黑盒场景，覆盖空实例、学习信号、三入口统一召回与审阅、反证、无损重组、迁移、备份恢复、协议兼容和纯离线基础闭环。
- Codex、OpenCode、Claude Code 通过真实 stdio MCP 公共接缝召回同一知识身份与版本，写入同构紧凑日志，并与 CLI 共享同一审阅状态；来源声明随召回返回并由入口 Skill 明确呈现。
- 基础安装不再依赖 Embedding；`sentence-transformers` 仅保留为可选 extra。断网、无模型包、无常驻进程时，捕获、反思、批准与召回闭环仍然通过。
- V1 规格标记为历史且由 V2 规格取代；V2 高层初始化行为测试替代了发布门禁中的 V1 内部布局依赖。协议能力、Schema 兼容矩阵与 V2.3 实现保持一致。
- `docs/releases/v2.md` 记录已发布能力、明确非目标和 V2 以后可选能力；未引入任何可选能力实现。
- Standards/Spec 双轴复审已完成并修复全部发现。最终严格 mypy 对 82 个源文件无问题，完整测试 288/288 通过。

## Comments

- 2026-07-19：发布 tracer bullet 完成；九项发布场景、严格 mypy、完整测试与双轴复审全绿。
