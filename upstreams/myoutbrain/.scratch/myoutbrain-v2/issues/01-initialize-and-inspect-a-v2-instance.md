# 01 — 初始化并检查 V2 实例

**What to build:** 创作者可以初始化一个干净的 V2 主私人实例，并通过最高层命令确认 Schema、规范存储、对象存储和单写者状态均可用。

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] 首次初始化建立带版本的 SQLite 规范状态和内容寻址对象存储。
- [x] 重复初始化保持幂等，不覆盖已存在数据或隐式迁移 V1 语义内容。
- [x] 状态命令通过稳定领域响应报告实例版本、写入可用性和完整性摘要。
- [x] 并发写入只允许一个写者，失败拥有稳定错误类别。
- [x] 黑盒测试证明中断写入只留下旧完整状态或新完整状态。

## Comments

- 2026-07-18：完成工单 01 tracer bullet。`init` 为新实例写入明确的 V2 实例标记，由本地核心在单写者锁内原子协调配置、SQLite 规范状态和托管忽略规则；无 V2 标记的 V1 工作空间会在写入前被拒绝，已标记 V2 的旧内部 Schema 仍按正常升级路径处理。
- 新增最高层 `status` CLI 领域响应，报告实例版本、SQLite 规范 Schema、单写者可用性和规范/内容寻址对象完整性；机器格式的锁竞争错误类别稳定为 `writer_locked`。V2 初始化默认关闭 Embedding，且不准备或下载模型。
- TDD 黑盒覆盖首次/重复初始化、V1 拒绝、并发写者、崩溃恢复、CAS 哈希损坏和状态响应。相关验证：26 个聚焦回归通过；严格 `mypy` 检查 54 个源文件通过；字节码与 diff 检查通过。
- 按 `code-review` 完成 Standards/Spec 双轴审查并修复实质发现：把跨文件初始化事务收回本地核心、消除 Schema 命名歧义与 CLI 序列化耦合、让中断测试先经状态接缝恢复并证明旧空状态、实际校验对象哈希，以及移除 V2 初始化的 Embedding 预置。
- 最终完整套件运行 170 个测试，其中 165 个通过；5 个公开检索时效测试失败。相同 5 个测试已在固定点 `84c3f53` 的独立临时 worktree 中逐一复现，确认是本工单之前的基线失败，未越界修改。
